<%- model_class = Qoute -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:message) %>:</strong></dt>
  <dd><%= @qoute.message %></dd>
  <dt><strong><%= model_class.human_attribute_name(:user_id) %>:</strong></dt>
  <dd><%= @qoute.user_id %></dd>
  <dt><strong><%= model_class.human_attribute_name(:qoute_by) %>:</strong></dt>
  <dd><%= @qoute.qoute_by %></dd>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              qoutes_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_qoute_path(@qoute), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              qoute_path(@qoute),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
